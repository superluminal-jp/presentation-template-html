<!-- SPECKIT START -->
- Prior features (implemented): 001 (core 8 layouts), 002 (extended 8, 16 total), 003 (component gallery + appendix), 004 (dashboard slides frame), 005 (deck UX hardening), 006 (DADS official HTML components intake), 007 (component-gallery deprecation & appendix consolidation)
- Active feature: 011 (editable-PPTX via DOM-measured extraction) — plan `specs/011-pptx-dom-extraction/plan.md`. Replaced the hand-written second source of truth in `scripts/pptx/build-pptx.mjs` (measured drift before: 28 HTML slides vs 20 builders, 208 duplicated JP literals, cover meta 20 vs 12 components) with: (1) a closed `data-pptx` role vocabulary on `index.html` (`text|shape|line|table|chart|image|group|ignore`); **the default is inverted from the plan** — body text is emitted automatically from any element with a direct non-whitespace text node, and `data-pptx` only OVERRIDES that (plan's opt-in default needed ~250 marks and made a forgotten mark silently drop text; inverted it needs 113 and fails loudly instead). Attributes must never be used in CSS selectors. (2) `scripts/pptx/{extract,emit,theme,group-xml}.mjs` — `extract` reads `getBoundingClientRect()`/`getComputedStyle()` from the live DOM (after frame.js chrome + tree-connectors.js lines are drawn) into a slide IR; it holds NO layout list and NO slide copy, so new slides follow with **zero converter edits** (proven: added a layout, it appeared in the PPTX with 0 lines changed under `scripts/pptx/`). (3) native `addTable`/`addChart`/bullets; pptxgenjs 4.0.1 has **no group API**, so `group-xml.mjs` post-processes `ppt/slides/slideN.xml` into `<p:grpSp>` (59 groups), bailing out to flat shapes if the shape run is unexpected — a valid file always wins. (4) `scripts/pptx/check-pptx.mjs` wired into `verify` (C1 slide count, C2 heading text, C3 data-chart consistency, C4 no raster), closing the "PPTX outside `verify`" hole. Falsification-tested: added slide → C1 fails; changed heading → C2 fails. `scripts/pptx/tokens.mjs` DELETED (colors resolved from CSS custom properties at extract time). `.quote::before` quotation mark moved to a real `<span class="quote__mark">`; `.checklist li::before` ✓ survives as a native PPTX bullet char (U+2713). Rendering is byte-identical before/after across all 28 slides. No new deps. `PPTX_CHROMIUM_PATH` env var overrides the Chromium executable for mismatched CI images.
- Active feature: 010 (tree-structure template — vertical & horizontal) — plan `specs/010-tree-template/plan.md`. Scope: reusable `.tree` component (semantic nested `<ul>/<li>` HTML nodes + **straight-line SVG connectors** drawn by `js/tree-connectors.js` from actual node offsets — 直線, not L-shaped/鍵線; role-variable colors only; equal-width grid so siblings fan out evenly) whose content model is Minto's Pyramid Principle (root=結論 / branch=MECE 根拠 / leaf=裏付け). Vertical (top-down) = NEW CORE layout `data-layout="tree"` (split `ORDER.tree='17'` → `slides/17-tree.html`, `LAYOUTS += '17-tree'`, after `process`); horizontal (left→right) = appendix `data-layout="appendix-tree-horizontal"` (`.tree--horizontal`). Both carry `data-practice="A-pyramid"` (no crossrefs/coverage churn). **Merged on top of 009**: appendix order timeline-2row(付録I) → tree(付録J) → grid(付録K); deck 26→28; dashboard 付録スライド tile 10→11; `deck.spec` count →28; new baselines `deck-tree`/`deck-appendix-tree-horizontal`/`17-tree`. `js/tree-connectors.js` loaded from `index.html` and the split foot (all `slides/*.html`). Invariants preserved: tokens-only, `@media print` single source, PDF pages==`.slide` count, `npm run verify` green, no new runtime deps. PPTX regenerated and verified by 011 (`check:pptx` now in `verify`).
- Prior feature: 009 (layout render fixes & composition unification) — plan `specs/009-layout-render-fixes/plan.md`. Scope: CSS-only fixes found via PDF review — legend swatch series-color specificity in `appendix.css`; dark-slide frame text (`.slide--image-full` → `--text-on-accent`); vertical-gravity/big-number; compare/chart/summary/dashboard fill; `.timeline` **redesigned to clear framed boxes** with state-colored time-pill headers (done/current/upcoming), step numbers/axis/arrows dropped; NEW appendix slide `appendix-timeline-2row` (2-row boxed timeline, 8 steps 4×2) → deck 25→26; inter-box connectors/turn arrows removed (order reads via ascending time pills + wrap). `deck.spec` updated 25→26.
- Prior feature: 008 (HTML→PDF confirmation flow for Claude Code) — plan `specs/008-html-pdf-preview/plan.md`. Scope: `npm run build:pdf` renders `index.html` → `dist/deck.pdf` (tracked in git via `!dist/deck.pdf`) reusing the existing `@media print` 1-slide-per-page rules; `npm run check:pdf` asserts PDF page count == live `.slide` count, wired into `verify`. Claude Code evaluates each slide against `contracts/evaluation-rubric.md`. No new runtime deps. New code under `scripts/pdf/`.
- Prior feature: 007 (component-gallery deprecation & appendix consolidation) — plan `specs/007-appendix-consolidation/plan.md`. Scope: delete the standalone `components.html` gallery and demonstrate all 20 components (14 self-authored + 6 DADS official) inside the deck appendix; `styles/dads-components.css` linked from `index.html`; deck component count reconciled 12 → 20; appendix slides added to the PPTX export (`scripts/pptx/build-pptx.mjs`). PPTX export lives under `scripts/pptx/` (`npm run build:pptx`).
- Requirements source: `docs/requirements.md`; practices catalog: `docs/practices.md`
- Canonical deck: `index.html`. `slides/*.html` are GENERATED by `scripts/split-slides.mjs` — never hand-edit; regenerate instead.

Active tech: static HTML/CSS + minimal vanilla JS; DS tokens `@digital-go-jp/design-tokens@2.0.1` (pinned, vendored, immutable) mapped via `styles/tokens.semantic.css`; validation with Playwright + axe-core + token lint. Present mode is opt-in (default `review`) so native-size tests stay green.
<!-- SPECKIT END -->

## パッケージ管理
Node/Python 等のライブラリはシステム全体ではなく、プロジェクトスコープでインストールすること。
- Node: `npm install <pkg>` / `npm install -D <pkg>`(`package.json`/`package-lock.json` に記録)。`npm install -g` は避ける。
- Python: venv 等のプロジェクトローカル環境にインストールし、システム Python への直接 `pip install` は避ける。
- 使い捨て実行(`npx <pkg>`, `uvx <pkg>` など)はこの限りではない。
