<!-- SPECKIT START -->
- Prior features (implemented): 001 (core 8 layouts), 002 (extended 8, 16 total), 003 (component gallery + appendix), 004 (dashboard slides frame), 005 (deck UX hardening)
- Active feature: 006 (DADS official HTML components intake) — plan `specs/006-dads-components/plan.md`. Scope: vendor 6 official DADS components (card/list/checkbox/progress-indicator/breadcrumb/radio) from upstream `digital-go-jp/design-system-example-components-html` at pinned SHA via `scripts/sync-dads.mjs` (`npm run sync:dads` / `check:dads`) into committed `vendor/dads-components/`; surface in `components.html` via new `styles/dads-components.css` (existing `components.css` untouched); official CSS resolves against existing `tokens/vendor/tokens.css`; no new runtime JS.
- Requirements source: `docs/requirements.md`; practices catalog: `docs/practices.md`
- Canonical deck: `index.html`. `slides/*.html` are GENERATED by `scripts/split-slides.mjs` — never hand-edit; regenerate instead.

Active tech: static HTML/CSS + minimal vanilla JS; DS tokens `@digital-go-jp/design-tokens@2.0.1` (pinned, vendored, immutable) mapped via `styles/tokens.semantic.css`; validation with Playwright + axe-core + token lint. Present mode is opt-in (default `review`) so native-size tests stay green.
<!-- SPECKIT END -->
