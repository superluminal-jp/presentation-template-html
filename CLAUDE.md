<!-- SPECKIT START -->
- Prior features (implemented): 001 (core 8 layouts), 002 (extended 8, 16 total), 003 (component gallery + appendix), 004 (dashboard slides frame)
- Active feature: 005 (deck UX hardening) — plan `specs/005-deck-ux-hardening/plan.md`. Scope: a11y (focus ring, single h1 + main landmark + slide names, non-color chart labels), accent emphasis discipline (≤1 accent focal per slide), opt-in present mode (`js/present.js`, `data-mode`, scale-to-fit, keyboard/fullscreen, reduced-motion), slide source drift check (`split-slides.mjs --check` → `npm run check:slides`).
- Requirements source: `docs/requirements.md`; practices catalog: `docs/practices.md`
- Canonical deck: `index.html`. `slides/*.html` are GENERATED by `scripts/split-slides.mjs` — never hand-edit; regenerate instead.

Active tech: static HTML/CSS + minimal vanilla JS; DS tokens `@digital-go-jp/design-tokens@2.0.1` (pinned, vendored, immutable) mapped via `styles/tokens.semantic.css`; validation with Playwright + axe-core + token lint. Present mode is opt-in (default `review`) so native-size tests stay green.
<!-- SPECKIT END -->
