<!-- SPECKIT START -->
- Prior features (implemented): 001 (core 8 layouts), 002 (extended 8, 16 total), 003 (component gallery + appendix), 004 (dashboard slides frame), 005 (deck UX hardening), 006 (DADS official HTML components intake), 007 (component-gallery deprecation & appendix consolidation)
- Active feature: 009 (layout render fixes & composition unification) — plan `specs/009-layout-render-fixes/plan.md`. Scope: CSS-only fixes found via PDF review — (P1) legend swatch series-color mismatch (raise specificity of `.legend__swatch--cat*` in `appendix.css` so it beats the later-loaded `.legend__swatch{background:accent}` base in `components.css`), and dark-slide frame text unreadable (add `.slide--image-full` to the `frame.css` inversion list → `--text-on-accent`); (P2) content sinks to vertical center — switch row-2 alignment to top (`summary/process/timeline/chart`) while preserving intentional centering (`section/quote/big-number`), and enlarge big-number via `calc(var(--font-size-64)*N)` (passes token-lint) + tighten number/anchor proximity; (P3 optional) sparse appendix F/H density, closing CTA wrap, title composition. Invariants unchanged: DS tokens/no-hardcode, `@media print` single source, PDF page count == live `.slide` count, `npm run verify` green. No new deps, no slide add/remove, HTML touched only for P3 CTA.
- Prior feature: 008 (HTML→PDF confirmation flow for Claude Code) — plan `specs/008-html-pdf-preview/plan.md`. Scope: `npm run build:pdf` renders `index.html` → `dist/deck.pdf` (tracked in git via `!dist/deck.pdf`, like `sample-deck.pptx`) reusing the existing `@media print` 1-slide-per-page rules (single source of truth, no new page CSS); `npm run check:pdf` deterministically asserts PDF page count == live `.slide` count (dynamic, no hardcode) and generation success, wired into `verify`. Claude Code (primary actor; human review secondary) reads the PDF via the Read tool `pages` param and evaluates each slide against `contracts/evaluation-rubric.md` — render integrity (clipping/page-split/frame-loss/overlap) + design-intent fit (DS palette, action titles, hierarchy, contrast); optional browser observation via Chromium. No new runtime deps (Playwright reused). New code lives in `scripts/pdf/`.
- Prior active feature: 007 (component-gallery deprecation & appendix consolidation) — plan `specs/007-appendix-consolidation/plan.md`. Scope: delete the standalone `components.html` gallery and demonstrate all 20 components (14 self-authored + 6 DADS official) inside the deck appendix. The 14 self-authored include two new DS-inspired additions — バナー(通知・緊急) and ステップナビゲーション (token-based, NOT via the `sync:dads` vendor pipeline). Appendix layout after consolidation: A (状態と通知, incl. divider) / B / C / D / E / F (プレゼン向け部品: banner + step-nav) / G (DADS official ①: card/list/breadcrumb) / H (DADS official ②: checkbox/radio/progress-indicator); `styles/dads-components.css` is linked from `index.html`. Kept `components.css`/`dads-components.css`/`tokens/vendor`/`vendor/dads-components`; the standalone gallery page and its two a11y tests were folded into the deck (index-level contrast + focus); deck component count reconciled 12 → 20; and appendix slides were added to the PPTX export (`scripts/pptx/build-pptx.mjs`, native shapes + `addTable`). Also note: PPTX export of the showcase deck now exists under `scripts/pptx/` (`npm run build:pptx`).
- Requirements source: `docs/requirements.md`; practices catalog: `docs/practices.md`
- Canonical deck: `index.html`. `slides/*.html` are GENERATED by `scripts/split-slides.mjs` — never hand-edit; regenerate instead.

Active tech: static HTML/CSS + minimal vanilla JS; DS tokens `@digital-go-jp/design-tokens@2.0.1` (pinned, vendored, immutable) mapped via `styles/tokens.semantic.css`; validation with Playwright + axe-core + token lint. Present mode is opt-in (default `review`) so native-size tests stay green.
<!-- SPECKIT END -->

## パッケージ管理
Node/Python 等のライブラリはシステム全体ではなく、プロジェクトスコープでインストールすること。
- Node: `npm install <pkg>` / `npm install -D <pkg>`(`package.json`/`package-lock.json` に記録)。`npm install -g` は避ける。
- Python: venv 等のプロジェクトローカル環境にインストールし、システム Python への直接 `pip install` は避ける。
- 使い捨て実行(`npx <pkg>`, `uvx <pkg>` など)はこの限りではない。
